name = "open-source-network-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "open-source-network"
database_id = "placeholder-replace-after-creation"

[vars]
RATE_LIMIT_FREE = "100"
RATE_LIMIT_PRO = "1000"

# Cron trigger: purge stale rate-limit windows every hour
# Prevents unbounded growth of the rate_limits D1 table.
[triggers]
crons = ["0 * * * *"]
