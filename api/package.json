{
  "name": "open-source-network-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API Gateway for Open Source Network tool database",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "db:apply": "wrangler d1 execute open-source-network --file=./schema.sql",
    "db:apply:remote": "wrangler d1 execute open-source-network --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.91.0"
  }
}
